name: grpc_annotation
description: Classes and helper functions that support gRPC code generation via the `grpc_gen` package.
version: 1.0.0
repository: https://github.com/xclud/dart_grpc_annotation
homepage: https://pwa.ir

environment:
  sdk: '>=2.12.0 <3.0.0'

dev_dependencies:
  test: ^1.22.0
  lints: ^2.0.0
